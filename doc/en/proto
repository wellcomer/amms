
 Communication protocol

 Amms modules are divided into the following types:

    sh - arguments are passed on STDIN, the answer is read from STDOUT.
    ammsc - arguments are passed in the URL encoded form.

  Requests:

     Separator argument is blank, the end of the transfer is \n.

  Answers:

     OK [description]
     ERR [description]
     Arbitrary data
