 Command line format:

 ammsc -u|--uri uri -p|--profile profile --exec|-e command

 uri - ammsd location, eg http://localhost/ammsd.fcgi
 command - command to execute

 Allowed abbreviated command, if it does not conflict with others.
 Command is the name of the extension or built-in ammsc command.

 Built-in commands (ammsc):

 Limits:

 limit [ident] [100M |no|state|desc|parent|rename|over] [1d|no] [temp|ident|parent]

 Set user limit:
 
    li user1 100M 1d

    Default limit type is ident.

 Set description

    li user1 desc SuperUser

 Remove description:

    li user1 desc no

 Set status (on, off, disabled)

    li user1 s on|off|dis

 Set parent limit (inheritation):

    li user1 parent users

 Rename user1 to user2:

    li user1 ren user2

 Set overlimit for 3 days:

    li user1 over 3d

 Force remove overlimit:

    li user1 over no

 Traffic:

 reset ident

 See extension for other commands
