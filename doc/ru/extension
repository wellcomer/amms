Профиль squid:

Расшифровка параметров модулей:

    -p - профиль
    -t - имя таблицы БД
    -u - uri
    -c - конфигурационный файл

Игнорируемые для подсчета адреса.

    ignore -p profile -t table -u matchd_uri [ls|rem|add] [num|rec]

    ls - просмотр
    rem num - удалить запись c порядковым номером num
    add rec - добавить запись rec

Импорт лимитов в БД

    import -p profile -c config_file filename

    filename - имя файла для импорта

    Для экспорта лимитов используйте команду
    ammsc li > filename

Объем использованного трафика

    stat uri ident

    uri - ammsd uri
    ident - идентификатор пользователя, если не указан выводится статистика по всем пользователям

История (журнал) посещаемых сайтов

    history -p profile -c config_file ident time [count|clear]

    ident - идентификатор пользователя
    time - время (regexp), если не указано выводится последние 25 записей
    count - количество выводимых записей
    clear - удаление истории

Список расширений

    ext -u ammsd_uri

Объем трафика пользователя за временной промежуток

    traf -p profile -c config_file ident time [detail]

    ident - идентификатор пользователя ( . - статистика по всем пользователям )
    time - время (regexp)
    detail - детализация

Привязка логина к IP адресу

    lock -p profile -t table -u hashd_uri [ls|set ident ip|rem num|get ident|check ident ip|help]

    ident - идентификатор пользователя
    ip - IP адрес
    num - удалить запись с порядковым номером num

