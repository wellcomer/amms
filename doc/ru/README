 Основные преимущества:

    Не изменяется конфиг. файл SQUID при изменении параметров доступа.
    Не требуется перезапуск SQUID при изменении параметров аккаунта.
    Управление с помощью командной строки (возможность вызова из скриптов).
    Web интерфейс.
    Управляемые события (превышение лимита, отключение лимита и т.п).
    Модули расширения могут быть написаны на любом языке программирования.
    Распределенная сетевая архитектура (легкое масштабирование и отказоустойчивость).
    No SQL.
    JSON API.
