 Формат командной строки:

 ammsc -u|--uri uri -p|--profile profile --exec|-e command

 uri - расположение ammsd, к примеру http://localhost/ammsd.fcgi
 profile - профиль
 command - команда для выполнения

 Допускается сокращенное написание команды, если оно не конфликтует с другими.
 Командой является имя расширения или встроенные в ammsc команды.

 Встроенные команды (ammsc):

 Лимиты:

 ammsс limit [ident] [100M |no|state|desc|parent|rename|over] [1d|no] [temp|ident|parent]

 Установить лимит пользователя user1, 100M на 24 часа с начала суток
 (таймер abs), с времени задания лимита (таймер rel)
 
    li user1 100M 1d

    Если тип лимита не указан, подразумевается ident.

 Задать описание

    li user1 desc SuperUser

 Удалить описание:

    li user1 desc no

 Изменить статус (включен, выключен, заблокирован)

    li user1 s on|off|dis

 Назначить родительский лимит (наследование недостающих параметров):

    li user1 parent users

 Переименовать user1 в user2:

    li user1 ren user2

 Задать неограниченный лимит на опр. период:

    li user1 over 3d

 Удалить неограниченный лимит вручную:

    li user1 over no

 Трафик:

 reset ident

 Остальные команды реализуются расширениями (см. extension)

